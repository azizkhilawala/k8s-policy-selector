import { a as __toESM } from "./rolldown-runtime-FZ4Itg2g.js";
import { t as require_react } from "./react.js";
import { t as require_jsx_runtime } from "./react_jsx-runtime.js";
import { M as props } from "./naming-Dl8QCPsP.js";
import { r as useLayer, t as useTooltip } from "./useTooltip-C7u2LzIY.js";
import { t as themeProps } from "./themeProps-C4uhFTd6.js";
import { i as unobserveResize, r as observeResize } from "./Text-CSQ7VcKr.js";
import { t as mergeProps } from "./mergeProps-D6YlC2X0.js";
import { t as mergeRefs } from "./mergeRefs-rZuiBi6r.js";
import { t as Button } from "./Button-Ch56vkDY.js";
import { t as useSize } from "./SizeContext-C2Xc_lPg.js";
import { n as Icon, r as renderIconSlot, t as useTranslator } from "./useTranslator-0ZeSsr5T.js";
import { t as usePopover } from "./usePopover-DvFy1bCv.js";
import { a as Field, i as inputWrapperStyles, l as useDevWarning, n as inputStatusFocusWithinStyles, r as inputStatusHoverShadowStyles, s as useAnnounce, t as inputStatusBorderStyles } from "./inputStyles.stylex-Bw2VZ_IR.js";
import { t as useIsomorphicLayoutEffect } from "./useIsomorphicLayoutEffect-DgEBZ1ZE.js";
import { t as Token } from "./Token-CZVGkWEI.js";
//#region node_modules/@astryxdesign/core/dist/utils/getKey.js
/**
* @file getKey.ts
* @input Receives an optional id key and required fallback key
* @output Exports getKey for stable React list keys
* @position Shared utility; consumed by components that render keyed lists
*/
function getKey(idKey, fallback) {
	if (idKey != null) return `id:${idKey}`;
	return `fallback:${typeof fallback === "function" ? fallback() : fallback}`;
}
//#endregion
//#region node_modules/@astryxdesign/core/dist/Field/InputClearButton.js
var import_jsx_runtime = require_jsx_runtime();
var styles$3 = { button: {
	kZKoxP: "x1qx5ct2",
	kmuXW: "x2lah0s",
	$$css: true
} };
function InputClearButton({ label, onClick, xstyle }) {
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Button, {
		variant: "ghost",
		size: "sm",
		label,
		icon: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Icon, {
			icon: "close",
			size: "sm",
			color: "inherit"
		}),
		onClick,
		isIconOnly: true,
		xstyle: [styles$3.button, xstyle]
	});
}
InputClearButton.displayName = "InputClearButton";
//#endregion
//#region node_modules/@astryxdesign/core/dist/Typeahead/TypeaheadItem.js
var import_react = /* @__PURE__ */ __toESM(require_react(), 1);
/**
* Default item component for typeahead dropdown results.
*
* Renders a label with optional icon and description.
* Exported for use in custom `renderItem` implementations.
*
* @example
* ```
* <Typeahead searchSource={source} value={v} onChange={setV} label="Search" />
* <Typeahead
*   searchSource={source}
*   value={v}
*   onChange={setV}
*   label="Search"
*   renderItem={(item) => (
*     <TypeaheadItem
*       item={item}
*       icon={<Avatar src={item.auxiliaryData.avatar} size="sm" />}
*       description={item.auxiliaryData.role}
*     />
*   )}
* />
* ```
*/
function TypeaheadItem({ ref, item, icon, description, isDisabled = false }) {
	if (item.element) return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: item.element });
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)("div", {
		ref,
		...mergeProps(themeProps("typeahead-item"), {
			0: { className: "x78zum5 x6s0dn4 x1txdalj x2lwn1j" },
			1: { className: "x78zum5 x6s0dn4 x1txdalj x2lwn1j xbyyjgo" }
		}[!!isDisabled << 0]),
		children: [icon, /*#__PURE__*/ (0, import_jsx_runtime.jsxs)("div", {
			className: "x78zum5 xdt5ytf x98rzlu xeuugli",
			children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)("span", {
				className: "xcr08ib x1kq96og x1sodnla x1tgivj0 xb3r6kr xlyipyv xuxw1ft",
				children: item.label
			}), description && /*#__PURE__*/ (0, import_jsx_runtime.jsx)("span", {
				className: "x141an7d x1ltkj2j xv1l7n4 xb3r6kr xlyipyv xuxw1ft",
				children: description
			})]
		})]
	});
}
TypeaheadItem.displayName = "TypeaheadItem";
//#endregion
//#region node_modules/@astryxdesign/core/dist/Typeahead/BaseTypeahead.js
/**
* @file BaseTypeahead.tsx
* @input Uses React, StyleX, usePopover, TypeaheadItem
* @output Exports BaseTypeahead combobox engine component
* @position Core implementation; used by Typeahead and Tokenizer
*
* Pure combobox engine: input, search, keyboard navigation, dropdown.
* No wrapper div, no border styling, no token rendering.
* Consumers provide their own wrapper and pass anchorRef for dropdown positioning.
*
* SYNC: When modified, update:
* - /packages/core/src/Typeahead/index.ts
* - /packages/cli/templates/blocks/components/Typeahead/ (showcase blocks)
*/
var styles$2 = {
	input: {
		k1xSpc: "x1lliihq",
		kUk6DE: "x98rzlu",
		k7Eaqz: "xfvyar9",
		kMzoRj: "xc342km",
		ksu8eU: "xng3xce",
		kmVPX3: "x1717udv",
		kMv6JI: "x9ynric",
		kGuDYH: "xjm74w1 x6pjikd",
		kLWn49: "xw6l6zx",
		kMwMTN: "x1tgivj0",
		kWkggS: "xjbqb8w",
		kI3sdo: "x1a2a7pz",
		k8Qsv1: "xeyghm5",
		$$css: true
	},
	inputDisabled: {
		kkrTdU: "x1h6gzvc",
		$$css: true
	},
	popover: {
		k7Eaqz: "xrzjruh",
		$$css: true
	},
	popoverGap: {
		keoZOQ: "xcsaf9d",
		k1K539: "x14cgwvg",
		$$css: true
	},
	item: {
		kB7OPa: "x9f619",
		k1xSpc: "x78zum5",
		kGNEyG: "x6s0dn4",
		kzqmXN: "xh8yej3",
		kmVPX3: "xlsj2fj",
		kaIpWk: "xh6dtrn",
		kkrTdU: "x1ypdohk",
		kI3sdo: "x1a2a7pz",
		kWkggS: "xjbqb8w",
		k9WMMc: "xdpxx8g",
		$$css: true
	},
	itemHighlighted: {
		kWkggS: "x1lmrjuc",
		$$css: true
	},
	itemSelected: {
		k63SB2: "x1e4wzip",
		$$css: true
	}
};
/**
* Size-specific overrides for dropdown list items.
* Matches the pattern used by DropdownMenuItem / Selector so that
* an `sm` typeahead renders compact list items.
*/
var itemSizeStyles = {
	sm: {
		k8WAf4: "xu0wf1k",
		kg3NbH: "xf314gf",
		$$css: true
	},
	md: {
		k8WAf4: "x1vofgu7",
		$$css: true
	},
	lg: {
		k8WAf4: "xce4md1",
		$$css: true
	}
};
/**
* Combobox engine: input, search, keyboard navigation, and dropdown.
*
* Renders only the `<input>` and the dropdown popover. No wrapper div,
* no border styling, no token rendering. Consumers (Typeahead,
* Tokenizer) provide their own wrapper and pass `anchorRef` for
* dropdown positioning.
*
* @example
* ```
* <BaseTypeahead
*   searchSource={source}
*   value={selected}
*   onChange={setSelected}
*   anchorRef={wrapperRef}
*   placeholder="Search..."
* />
* ```
*/
var BaseTypeahead = function BaseTypeahead({ searchSource, value, onChange, renderItem, placeholder: placeholderFromProps, hasEntriesOnFocus = false, maxMenuItems = 10, emptySearchResultsText: emptySearchResultsTextFromProps, isDisabled = false, isFocusableDisabled = false, hasAutoFocus = false, onChangeQuery, onOpenChange, inputId: externalInputId, ariaDescribedBy, ariaLabelledBy, inputXStyle, anchorRef, onKeyDown: externalOnKeyDown, debounceMs = 150, size = "md", ref }) {
	const t = useTranslator();
	const placeholder = placeholderFromProps ?? t("@astryx.typeahead.searchPlaceholder");
	const emptySearchResultsText = emptySearchResultsTextFromProps ?? t("@astryx.typeahead.emptySearchResults");
	const generatedId = (0, import_react.useId)();
	const inputId = externalInputId ?? generatedId;
	const listboxId = (0, import_react.useId)();
	const inputRef = (0, import_react.useRef)(null);
	const fallbackAnchorRef = (0, import_react.useRef)(null);
	const announce = useAnnounce();
	const [query, setQuery] = (0, import_react.useState)("");
	const [results, setResults] = (0, import_react.useState)([]);
	const [highlightedIndex, setHighlightedIndex] = (0, import_react.useState)(-1);
	const [isLoading, setIsLoading] = (0, import_react.useState)(false);
	const [hasSearched, setHasSearched] = (0, import_react.useState)(false);
	const pointerActiveRef = (0, import_react.useRef)(false);
	const searchTimeoutRef = (0, import_react.useRef)(null);
	const searchGenRef = (0, import_react.useRef)(0);
	const resultsGenRef = (0, import_react.useRef)(0);
	const popover = usePopover({
		onShow: (0, import_react.useCallback)(() => {
			onOpenChange?.(true);
		}, [onOpenChange]),
		onHide: (0, import_react.useCallback)(() => {
			onOpenChange?.(false);
			setHighlightedIndex(-1);
			searchSource.cancel?.();
		}, [onOpenChange, searchSource]),
		hasLightDismiss: true,
		hasCloseButton: false,
		hasAutoFocus: false,
		role: "none"
	});
	const showLayer = (0, import_react.useCallback)(() => {
		if (pointerActiveRef.current) document.addEventListener("click", () => requestAnimationFrame(() => popover.show()), { once: true });
		else popover.show();
	}, [popover]);
	(0, import_react.useEffect)(() => {
		const el = anchorRef?.current ?? fallbackAnchorRef.current;
		if (el) popover.triggerRef(el);
		return () => {
			popover.triggerRef(null);
		};
	}, [popover, anchorRef]);
	const performSearch = (0, import_react.useCallback)(async (searchQuery) => {
		searchSource.cancel?.();
		const gen = ++searchGenRef.current;
		setIsLoading(true);
		setHasSearched(true);
		try {
			const searchResults = await searchSource.search(searchQuery);
			if (searchGenRef.current !== gen) return;
			resultsGenRef.current = gen;
			const shown = searchResults.slice(0, maxMenuItems);
			setResults(shown);
			setHighlightedIndex(searchResults.length > 0 ? 0 : -1);
			if (searchResults.length > 0 || searchQuery.length > 0) showLayer();
			if (searchQuery.length > 0) announce(shown.length === 0 ? emptySearchResultsText : `${shown.length} ${shown.length === 1 ? "result" : "results"}`);
		} catch {
			if (searchGenRef.current !== gen) return;
			setResults([]);
			setHighlightedIndex(-1);
		} finally {
			if (searchGenRef.current === gen) setIsLoading(false);
		}
	}, [
		searchSource,
		maxMenuItems,
		showLayer,
		announce,
		emptySearchResultsText
	]);
	const performBootstrap = (0, import_react.useCallback)(async () => {
		const gen = ++searchGenRef.current;
		setIsLoading(true);
		try {
			const bootstrapResults = await searchSource.bootstrap();
			if (searchGenRef.current !== gen) return;
			resultsGenRef.current = gen;
			setResults(bootstrapResults.slice(0, maxMenuItems));
			setHighlightedIndex(bootstrapResults.length > 0 ? 0 : -1);
			if (bootstrapResults.length > 0) showLayer();
		} catch {
			if (searchGenRef.current !== gen) return;
			setResults([]);
		} finally {
			if (searchGenRef.current === gen) setIsLoading(false);
		}
	}, [
		searchSource,
		maxMenuItems,
		showLayer
	]);
	const handleQueryChange = (0, import_react.useCallback)((newQuery) => {
		setQuery(newQuery);
		onChangeQuery?.(newQuery);
		if (searchTimeoutRef.current) clearTimeout(searchTimeoutRef.current);
		if (newQuery.length === 0 && !hasEntriesOnFocus) {
			searchGenRef.current++;
			searchSource.cancel?.();
			setResults([]);
			setHasSearched(false);
			announce("");
			popover.hide();
			return;
		}
		const triggerSearch = () => {
			if (newQuery.length > 0) performSearch(newQuery);
			else if (hasEntriesOnFocus) performBootstrap();
		};
		if (debounceMs <= 0) triggerSearch();
		else searchTimeoutRef.current = setTimeout(triggerSearch, debounceMs);
	}, [
		onChangeQuery,
		hasEntriesOnFocus,
		performSearch,
		performBootstrap,
		popover,
		debounceMs,
		searchSource,
		announce
	]);
	const handleInputChange = (0, import_react.useCallback)((e) => {
		handleQueryChange(e.target.value);
	}, [handleQueryChange]);
	const handleSelect = (0, import_react.useCallback)((item) => {
		searchGenRef.current++;
		if (searchTimeoutRef.current) {
			clearTimeout(searchTimeoutRef.current);
			searchTimeoutRef.current = null;
		}
		searchSource.cancel?.();
		onChange(item);
		setQuery("");
		setResults([]);
		setHasSearched(false);
		popover.hide();
		inputRef.current?.focus();
	}, [
		onChange,
		popover,
		searchSource
	]);
	const handleFocus = (0, import_react.useCallback)(() => {
		if (isDisabled) return;
		if (hasEntriesOnFocus && results.length === 0 && query.length === 0) performBootstrap();
		else if (results.length > 0 && (query.length > 0 || hasEntriesOnFocus) && resultsGenRef.current === searchGenRef.current) showLayer();
	}, [
		isDisabled,
		hasEntriesOnFocus,
		results.length,
		query.length,
		performBootstrap,
		showLayer
	]);
	const handleBlur = (0, import_react.useCallback)((e) => {
		if (!popover.isOpen) return;
		const next = e.relatedTarget;
		if (next) {
			const anchorEl = anchorRef?.current ?? fallbackAnchorRef.current;
			const popoverEl = document.getElementById(popover.id);
			if (anchorEl?.contains(next) || popoverEl?.contains(next)) return;
		}
		popover.hide();
	}, [popover, anchorRef]);
	const handleKeyDown = (0, import_react.useCallback)((e) => {
		externalOnKeyDown?.(e);
		if (e.defaultPrevented) return;
		if (!popover.isOpen) {
			if (e.key === "ArrowDown" && (hasEntriesOnFocus || query.length > 0)) {
				e.preventDefault();
				if (results.length > 0) {
					popover.show();
					setHighlightedIndex(0);
				} else if (hasEntriesOnFocus) performBootstrap();
			}
			return;
		}
		switch (e.key) {
			case "ArrowDown":
				e.preventDefault();
				setHighlightedIndex((prev) => prev < results.length - 1 ? prev + 1 : 0);
				break;
			case "ArrowUp":
				e.preventDefault();
				setHighlightedIndex((prev) => prev > 0 ? prev - 1 : results.length - 1);
				break;
			case "Enter":
				e.preventDefault();
				if (highlightedIndex >= 0 && highlightedIndex < results.length) handleSelect(results[highlightedIndex]);
				break;
			case "Escape":
				e.preventDefault();
				popover.hide();
				break;
			case "Home":
				if (popover.isOpen) {
					e.preventDefault();
					setHighlightedIndex(0);
				}
				break;
			case "End":
				if (popover.isOpen) {
					e.preventDefault();
					setHighlightedIndex(results.length - 1);
				}
				break;
		}
	}, [
		popover,
		results,
		highlightedIndex,
		handleSelect,
		hasEntriesOnFocus,
		query.length,
		performBootstrap,
		externalOnKeyDown
	]);
	const getItemId = (0, import_react.useCallback)((index) => `${listboxId}-option-${index}`, [listboxId]);
	(0, import_react.useEffect)(() => {
		if (!popover.isOpen || highlightedIndex < 0) return;
		document.getElementById(getItemId(highlightedIndex))?.scrollIntoView?.({ block: "nearest" });
	}, [
		popover.isOpen,
		highlightedIndex,
		getItemId
	]);
	const selectedKey = value == null ? null : getKey(value.id, () => results.indexOf(value));
	(0, import_react.useEffect)(() => {
		return () => {
			if (searchTimeoutRef.current) clearTimeout(searchTimeoutRef.current);
			searchSource.cancel?.();
		};
	}, [searchSource]);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
		/*#__PURE__*/ (0, import_jsx_runtime.jsx)("input", {
			ref: mergeRefs(ref, inputRef, fallbackAnchorRef),
			id: inputId,
			type: "text",
			role: "combobox",
			"aria-expanded": popover.isOpen,
			"aria-controls": listboxId,
			"aria-activedescendant": popover.isOpen && highlightedIndex >= 0 ? getItemId(highlightedIndex) : void 0,
			"aria-autocomplete": "list",
			"aria-describedby": ariaDescribedBy,
			"aria-labelledby": ariaLabelledBy,
			"aria-disabled": isFocusableDisabled ? "true" : void 0,
			value: query,
			onChange: handleInputChange,
			onPointerDown: () => {
				pointerActiveRef.current = true;
				document.addEventListener("click", () => {
					pointerActiveRef.current = false;
				}, { once: true });
			},
			onFocus: handleFocus,
			onBlur: handleBlur,
			onKeyDown: handleKeyDown,
			placeholder,
			disabled: isDisabled && !isFocusableDisabled,
			readOnly: isFocusableDisabled || void 0,
			autoFocus: hasAutoFocus,
			"data-autofocus": hasAutoFocus || void 0,
			autoComplete: "off",
			...props(styles$2.input, isDisabled && styles$2.inputDisabled, inputXStyle)
		}),
		isLoading && /*#__PURE__*/ (0, import_jsx_runtime.jsx)("span", {
			role: "status",
			"aria-label": t("@astryx.typeahead.loading"),
			className: "x3nfvp2 x6s0dn4 xl56j7k x9epnlk",
			children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Icon, {
				icon: "clock",
				size: "sm",
				color: "secondary"
			})
		}),
		popover.render(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("div", {
			id: listboxId,
			role: "listbox",
			"aria-label": t("@astryx.typeahead.searchResults"),
			...mergeProps(themeProps("typeahead-dropdown"), { className: "x9f619 xuyqlj2 x1odjw0f x9epnlk" }),
			children: results.length === 0 && hasSearched ? /*#__PURE__*/ (0, import_jsx_runtime.jsx)("div", {
				className: "x1b2ylru x2b8uid x141an7d xv1l7n4",
				children: emptySearchResultsText
			}) : results.map((item, index) => {
				const itemKey = getKey(item.id, index);
				const isSelected = itemKey === selectedKey;
				return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)("div", {
					id: getItemId(index),
					role: "option",
					"aria-selected": isSelected,
					tabIndex: -1,
					onClick: () => handleSelect(item),
					onMouseEnter: () => setHighlightedIndex(index),
					...props(styles$2.item, itemSizeStyles[size], index === highlightedIndex && styles$2.itemHighlighted, isSelected && styles$2.itemSelected),
					children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)("span", {
						className: "x78zum5 x98rzlu xeuugli",
						children: renderItem ? renderItem(item) : /*#__PURE__*/ (0, import_jsx_runtime.jsx)(TypeaheadItem, { item })
					}), isSelected && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Icon, {
						icon: "check",
						size: "sm",
						color: "primary"
					})]
				}, itemKey);
			})
		}), {
			placement: "below",
			alignment: "start",
			xstyle: [styles$2.popover, styles$2.popoverGap]
		})
	] });
};
BaseTypeahead.displayName = "BaseTypeahead";
//#endregion
//#region node_modules/@astryxdesign/core/dist/hooks/computeOverflow.js
/**
* @file computeOverflow.ts
* @input Plain measurements (item widths, gap, available width, indicator width, limits)
* @output Pure overflow computation: how many items are visible and their row layout
* @position Pure helper consumed by useOverflow; contains no DOM or React code so the
*           fit/clamp math and the multi-row packing can be unit-tested in isolation.
*
* There is a single source of truth for `visibleCount`:
*   visibleCount = clamp(fitCount, minVisibleItems, maxVisibleItems ?? itemCount)
*
* SYNC: When modified, update:
* - /packages/core/src/hooks/useOverflow.ts (the only caller)
*/
function clamp(value, min, max) {
	return Math.max(Math.min(value, max), min);
}
/**
* Resolve the ceiling and floor into a single [floor, ceiling] pair.
*
* `maxVisibleItems` is clamped to `itemCount` (a cap larger than the list is a
* no-op). When `maxVisibleItems < minVisibleItems`, the floor wins (D1) via the
* final clamp — the hook is responsible for the dev-only warning.
*/
function resolveBounds(itemCount, minVisibleItems, maxVisibleItems) {
	return {
		floor: Math.max(0, Math.min(minVisibleItems, itemCount)),
		ceiling: Math.max(0, Math.min(maxVisibleItems == null ? itemCount : maxVisibleItems, itemCount))
	};
}
/**
* Single-line greedy fit: how many items fit in one row of `availableWidth`,
* reserving indicator space for every non-final admitted item. Mirrors the
* original in-hook loop so single-line behavior is unchanged.
*/
function computeSingleLineFit(orderedWidths, gap, availableWidth, indicatorWidth, floor, ceiling) {
	let totalWidth = 0;
	let count = 0;
	for (let i = 0; i < orderedWidths.length; i++) {
		if (count >= ceiling) break;
		const itemWidth = orderedWidths[i];
		const gapWidth = i > 0 ? gap : 0;
		const candidateWidth = totalWidth + itemWidth + gapWidth;
		if (candidateWidth + (i === orderedWidths.length - 1 ? 0 : indicatorWidth + (count > 0 || indicatorWidth > 0 ? gap : 0)) > availableWidth && count >= floor) break;
		totalWidth = candidateWidth;
		count++;
	}
	return count;
}
/**
* Pack items into rows, wrapping when the next item would exceed
* `availableWidth`. On the last allowed row, keep `indicatorReserve` px free so
* the overflow indicator fits. Stops admitting once no further item can be
* placed within `maxRows`.
*/
function packRows(orderedWidths, gap, availableWidth, indicatorReserve, maxRows) {
	let placed = 0;
	let row = 1;
	let rowWidth = 0;
	for (let i = 0; i < orderedWidths.length; i++) {
		const w = orderedWidths[i];
		const isFirstInRow = rowWidth === 0;
		const candidate = isFirstInRow ? w : rowWidth + gap + w;
		const onLastRow = row === maxRows;
		const reserve = onLastRow && indicatorReserve > 0 ? indicatorReserve + gap : 0;
		if (candidate + reserve <= availableWidth) {
			rowWidth = candidate;
			placed++;
			continue;
		}
		if (isFirstInRow) {
			if (onLastRow && reserve > 0) break;
			rowWidth = candidate;
			placed++;
			continue;
		}
		if (row >= maxRows) break;
		row++;
		rowWidth = 0;
		i--;
	}
	return {
		placed,
		rows: row
	};
}
/** Count how many rows a set of items occupies when wrapped at availableWidth. */
function countRows(orderedWidths, gap, availableWidth) {
	if (orderedWidths.length === 0) return 0;
	let rows = 1;
	let rowWidth = 0;
	for (let i = 0; i < orderedWidths.length; i++) {
		const w = orderedWidths[i];
		const isFirstInRow = rowWidth === 0;
		const candidate = isFirstInRow ? w : rowWidth + gap + w;
		if (candidate <= availableWidth || isFirstInRow) rowWidth = candidate;
		else {
			rows++;
			rowWidth = w;
		}
	}
	return rows;
}
/**
* Multi-row packing (Strategy A): simulate wrapping items into rows of
* `availableWidth`, admitting items until a new item would require a row beyond
* `maxRows`, reserving indicator space on the final row when items overflow.
* Returns the admitted count and how many rows those items occupy. Assumes
* uniform row height.
*/
function computeMultiRowFit(orderedWidths, gap, availableWidth, indicatorWidth, maxRows) {
	const n = orderedWidths.length;
	if (n === 0) return {
		count: 0,
		rows: 0
	};
	const packAll = packRows(orderedWidths, gap, availableWidth, 0, maxRows);
	if (packAll.placed === n) return {
		count: n,
		rows: packAll.rows
	};
	const count = packRows(orderedWidths, gap, availableWidth, indicatorWidth, maxRows).placed;
	const rows = countRows(orderedWidths.slice(0, count), gap, availableWidth);
	return {
		count,
		rows: Math.max(count > 0 ? 1 : 0, rows)
	};
}
/**
* Pure overflow computation. Given plain measurements and the configured
* limits, return how many items should be visible and how many rows they
* occupy. No DOM, no React — safe to unit-test directly.
*/
function computeOverflow(input) {
	const { widths, gap, availableWidth, indicatorWidth, minVisibleItems, maxVisibleItems, maxRows, collapseFrom } = input;
	const itemCount = widths.length;
	if (itemCount === 0) return {
		visibleCount: 0,
		rows: 0
	};
	const { floor, ceiling } = resolveBounds(itemCount, minVisibleItems, maxVisibleItems);
	const orderedWidths = collapseFrom === "end" ? widths : [...widths].reverse();
	if (!(maxRows != null && maxRows > 1)) {
		const visibleCount = clamp(computeSingleLineFit(orderedWidths, gap, availableWidth, indicatorWidth, floor, ceiling), floor, ceiling);
		return {
			visibleCount,
			rows: visibleCount > 0 ? 1 : 0
		};
	}
	const { count, rows } = computeMultiRowFit(orderedWidths, gap, availableWidth, indicatorWidth, maxRows);
	const visibleCount = clamp(count, floor, ceiling);
	const resolvedRows = visibleCount === count ? rows : countRows(orderedWidths.slice(0, visibleCount), gap, availableWidth);
	return {
		visibleCount,
		rows: visibleCount > 0 ? Math.max(1, resolvedRows) : 0
	};
}
//#endregion
//#region node_modules/@astryxdesign/core/dist/hooks/useOverflow.js
/**
* @file useOverflow.ts
* @input Uses React useState, useCallback, useRef; useIsomorphicLayoutEffect
* @output Exports useOverflow hook for measuring and managing horizontal overflow
* @position Core hook; used by OverflowList and consumers for overflow patterns
*
* Measures children rendered in a hidden container to determine how many fit
* in the available width, without flickering. Uses ResizeObserver to react
* to container size changes. Supports an optional item cap (`maxVisibleItems`)
* and bounded multi-row wrapping (`maxRows`); the fit/clamp/row-packing math
* lives in the pure `computeOverflow` helper.
*
* SYNC: When modified, update:
* - /packages/core/src/hooks/index.ts
* - /packages/core/src/hooks/computeOverflow.ts (the pure computation)
*/
/**
* Hook for managing horizontal overflow of a list of items.
*
* Renders all items into a hidden measurement container, then calculates
* how many fit in the visible container's width. Uses ResizeObserver to
* recalculate when the container resizes.
*
* The measurement container should contain all items followed by the
* overflow indicator element (if any). The indicator is identified by
* a `data-overflow-indicator` attribute.
*
* @example
* ```
* const { containerRef, measureRef, visibleCount, hasOverflow } = useOverflow(5, {
*   gap: 8,
* });
* ```
*/
function useOverflow(itemCount, options = {}) {
	const { gap = 0, minVisibleItems = 0, maxVisibleItems, maxRows, collapseFrom = "end", behavior = "observeSelf" } = options;
	useDevWarning("useOverflow", `maxVisibleItems (${maxVisibleItems}) is less than minVisibleItems (${minVisibleItems}); the floor wins and minVisibleItems items will be shown.`, maxVisibleItems != null && maxVisibleItems < minVisibleItems);
	const observeParent = behavior === "observeParent";
	const [visibleCount, setVisibleCount] = (0, import_react.useState)(itemCount);
	const [rows, setRows] = (0, import_react.useState)(1);
	const [rowHeight, setRowHeight] = (0, import_react.useState)(0);
	const containerElRef = (0, import_react.useRef)(null);
	const measureElRef = (0, import_react.useRef)(null);
	const observedElRef = (0, import_react.useRef)(null);
	const calculate = (0, import_react.useCallback)(() => {
		const container = containerElRef.current;
		const measure = measureElRef.current;
		if (!container || !measure) return;
		let availableWidth;
		if (observeParent && container.parentElement) {
			const parent = container.parentElement;
			const parentStyle = getComputedStyle(parent);
			availableWidth = parent.clientWidth - parseFloat(parentStyle.paddingLeft) - parseFloat(parentStyle.paddingRight);
		} else availableWidth = container.offsetWidth;
		const allChildren = Array.from(measure.children);
		const hasIndicator = allChildren.length > itemCount;
		const children = hasIndicator ? allChildren.slice(0, itemCount) : allChildren;
		const indicatorWidth = hasIndicator ? allChildren[allChildren.length - 1].offsetWidth : 0;
		if (children.length === 0) {
			setVisibleCount(0);
			setRows(0);
			return;
		}
		const widths = children.map((child) => child.offsetWidth);
		const measuredRowHeight = children.reduce((max, child) => Math.max(max, child.offsetHeight || 0), 0);
		const { visibleCount: nextVisible, rows: nextRows } = computeOverflow({
			widths,
			gap,
			availableWidth,
			indicatorWidth,
			minVisibleItems,
			maxVisibleItems,
			maxRows,
			collapseFrom
		});
		setVisibleCount(nextVisible);
		setRows((prev) => prev === nextRows ? prev : nextRows);
		setRowHeight((prev) => prev === measuredRowHeight ? prev : measuredRowHeight);
	}, [
		itemCount,
		gap,
		minVisibleItems,
		maxVisibleItems,
		maxRows,
		collapseFrom,
		observeParent
	]);
	const containerRef = (0, import_react.useCallback)((el) => {
		containerElRef.current = el;
		if (observedElRef.current) {
			unobserveResize(observedElRef.current);
			observedElRef.current = null;
		}
		if (el) {
			const target = observeParent && el.parentElement ? el.parentElement : el;
			observeResize(target, () => {
				calculate();
			});
			observedElRef.current = target;
		}
	}, [calculate, observeParent]);
	const measureRef = (0, import_react.useCallback)((el) => {
		measureElRef.current = el;
		if (el) calculate();
	}, [calculate]);
	useIsomorphicLayoutEffect(() => {
		calculate();
	}, [calculate]);
	return {
		containerRef,
		measureRef,
		visibleCount,
		hasOverflow: visibleCount < itemCount,
		rows,
		rowHeight
	};
}
//#endregion
//#region node_modules/@astryxdesign/core/dist/OverflowList/OverflowList.js
/**
* @file OverflowList.tsx
* @input Uses React, StyleX, useOverflow hook
* @output Exports OverflowList component and OverflowListProps type
* @position Core implementation; consumed by index.ts
*
* Renders a horizontal list of items, hiding those that don't fit in the
* available width and optionally showing an overflow indicator. Supports an
* optional item cap (`maxVisibleItems`) and bounded multi-row wrapping
* (`maxRows`). Uses a hidden measurement container to avoid flickering.
*
* SYNC: When modified, update these files to stay in sync:
* - /packages/core/src/OverflowList/index.ts (exports if types change)
* - /packages/cli/templates/blocks/components/OverflowList/ (showcase blocks)
*/
var styles$1 = {
	container: {
		k1xSpc: "x78zum5",
		kGNEyG: "x6s0dn4",
		kVQacm: "xb3r6kr",
		khDVqt: "xuxw1ft",
		k7Eaqz: "xeuugli",
		$$css: true
	},
	containerMultiRow: {
		k1xSpc: "x78zum5",
		kwnvtZ: "x1a02dak",
		kfiyM8: "x8gbvx8",
		kVQacm: "xb3r6kr",
		khDVqt: "xeaf4i8",
		k7Eaqz: "xeuugli",
		$$css: true
	},
	fillParent: {
		kzqmXN: "xh8yej3",
		$$css: true
	},
	measureContainer: {
		kVAEAm: "x10l6tqk",
		k33iCy: "xlshs6z",
		kZKoxP: "xqtp20y",
		kVQacm: "xb3r6kr",
		k1xSpc: "x78zum5",
		kGNEyG: "x6s0dn4",
		khDVqt: "xuxw1ft",
		kfzvcC: "x47corl",
		$$css: true
	}
};
var _temp = {
	kskxy: "x1jols5v",
	"$$css": true
};
var multiRowHeight = { height: (maxRows, rowHeight, gapPx) => [_temp, { "--x-maxHeight": ((val) => typeof val === "number" ? val + "px" : val != null ? val : void 0)(`calc(${rowHeight}px * ${maxRows} + ${gapPx}px * ${maxRows - 1})`) }] };
var gapStyles = {
	"0": {
		kOIVth: "xsn7fz1",
		$$css: true
	},
	"1": {
		kOIVth: "xzye2dw",
		$$css: true
	},
	"2": {
		kOIVth: "x1txdalj",
		$$css: true
	},
	"3": {
		kOIVth: "xjcht0a",
		$$css: true
	},
	"4": {
		kOIVth: "x18g69wz",
		$$css: true
	},
	"5": {
		kOIVth: "x9mgr7n",
		$$css: true
	},
	"6": {
		kOIVth: "x1qh66ti",
		$$css: true
	},
	"8": {
		kOIVth: "x4t41sb",
		$$css: true
	},
	"10": {
		kOIVth: "x3hoi3v",
		$$css: true
	},
	"0.5": {
		kOIVth: "x1lsbc85",
		$$css: true
	},
	"1.5": {
		kOIVth: "x1s4dlld",
		$$css: true
	}
};
/**
* Maps spacing token steps to pixel values for overflow calculations.
*/
var spacingToPx = {
	0: 0,
	.5: 2,
	1: 4,
	1.5: 6,
	2: 8,
	3: 12,
	4: 16,
	5: 20,
	6: 24,
	8: 32,
	10: 40
};
/**
* A horizontal list that hides items that don't fit and shows an overflow indicator.
*
* Uses a hidden measurement container to determine which items fit without
* causing visual flickering. The overflow indicator is also measured
* automatically so no manual width value is needed.
*
* @example
* ```
* <OverflowList
*   gap={2}
*   overflowRenderer={(items) => (
*     <Button label={`+${items.length} more`} variant="ghost" />
*   )}>
*   <Button label="Action 1" />
*   <Button label="Action 2" />
*   <Button label="Action 3" />
*   <Button label="Action 4" />
* </OverflowList>
* ```
*/
function OverflowList({ children, gap = 2, minVisibleItems = 0, maxVisibleItems, maxRows, collapseFrom = "end", behavior = "observeSelf", overflowRenderer, xstyle, className, style, ref, ...props$1 }) {
	const childArray = import_react.Children.toArray(children);
	const itemCount = childArray.length;
	const gapPx = spacingToPx[gap];
	const observeParent = behavior === "observeParent";
	const isMultiRow = maxRows != null && maxRows > 1;
	const { containerRef, measureRef, visibleCount, hasOverflow, rowHeight } = useOverflow(itemCount, {
		gap: gapPx,
		minVisibleItems,
		maxVisibleItems,
		maxRows,
		collapseFrom,
		behavior
	});
	const allItems = childArray.map((child, index) => ({
		child,
		index
	}));
	let visibleItems;
	let overflowItems;
	if (collapseFrom === "end") {
		visibleItems = allItems.slice(0, visibleCount);
		overflowItems = allItems.slice(visibleCount);
	} else {
		visibleItems = allItems.slice(itemCount - visibleCount);
		overflowItems = allItems.slice(0, itemCount - visibleCount);
	}
	const measureIndicator = overflowRenderer?.(allItems);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/*#__PURE__*/ (0, import_jsx_runtime.jsxs)("div", {
		ref: measureRef,
		"aria-hidden": "true",
		inert: true,
		...props(styles$1.measureContainer, gapStyles[gap]),
		children: [childArray, measureIndicator != null && /*#__PURE__*/ (0, import_jsx_runtime.jsx)("div", {
			className: "x3nfvp2",
			children: measureIndicator
		})]
	}), /*#__PURE__*/ (0, import_jsx_runtime.jsxs)("div", {
		ref: mergeRefs(ref, containerRef),
		...mergeProps(themeProps("overflow-list"), props(isMultiRow ? styles$1.containerMultiRow : styles$1.container, gapStyles[gap], isMultiRow && rowHeight > 0 && maxRows != null && multiRowHeight.height(maxRows, rowHeight, gapPx), observeParent && hasOverflow && styles$1.fillParent, xstyle), className, style),
		...props$1,
		children: [
			collapseFrom === "start" && hasOverflow && overflowRenderer?.(overflowItems),
			visibleItems.map(({ child }) => child),
			collapseFrom === "end" && hasOverflow && overflowRenderer?.(overflowItems)
		]
	})] });
}
OverflowList.displayName = "OverflowList";
//#endregion
//#region node_modules/@astryxdesign/core/dist/Tokenizer/Tokenizer.js
/**
* @file Tokenizer.tsx
* @input Uses React, BaseTypeahead, Field, Token, useAnnounce
* @output Exports Tokenizer multi-select typeahead component
* @position Composed component; forwards DOM ref and exposes focus control via
*   handleRef
*
* SYNC: When modified, update:
* - /packages/core/src/Tokenizer/index.ts
* - /apps/storybook/stories/Tokenizer.stories.tsx
* - /packages/cli/templates/blocks/components/Tokenizer/ (showcase blocks)
*/
/**
* Change metadata for onChange callback.
*/
/**
* Controls overflow behavior when tokens exceed the available width.
* - `'none'`: All tokens wrap normally (default).
* - `'unfocusedInline'`: Shows a single line with "+ N more" when unfocused, expands inline on focus.
* - `'unfocusedLayer'`: Shows a single line with "+ N more" when unfocused, expands as an overlay on focus.
*/
/**
* Imperative handle for Tokenizer handleRef.
*/
var styles = {
	wrapper: {
		kVAEAm: "x1n2onr6",
		kwnvtZ: "x1a02dak",
		kOIVth: "xzye2dw",
		kkrTdU: "x1ed109x",
		kZKoxP: "xt7dq6l",
		$$css: true
	},
	wrapperWithTokens: {
		k8WAf4: "x1x7cmac",
		kg3NbH: "xx2oswg",
		khm7nJ: "xajebq8",
		$$css: true
	},
	token: {
		k1xSpc: "x78zum5",
		kmuXW: "x2lah0s",
		$$css: true
	},
	endSection: {
		kVAEAm: "x10l6tqk",
		kt4wiu: "x72tfeb",
		k1xSpc: "x78zum5",
		kGNEyG: "x6s0dn4",
		kOIVth: "x1txdalj",
		kmuXW: "x2lah0s",
		$$css: true
	},
	inputAtMax: {
		kzqmXN: "xnalus7",
		k7Eaqz: "xeuugli",
		kUk6DE: "x1somy94",
		kmVPX3: "x1717udv",
		kSiTet: "xg01cxk",
		kVAEAm: "x10l6tqk",
		$$css: true
	},
	inputCompact: {
		k7Eaqz: "x1fns5xo",
		kUk6DE: "xfoc8du",
		kzqmXN: "xnalus7",
		kZCmMZ: "x2a0j69",
		$$css: true
	},
	truncatedWrapper: {
		kwnvtZ: "xozqiw3",
		kVQacm: "xb3r6kr",
		$$css: true
	},
	layerPopover: {
		kzqmXN: "x1xgy17j",
		$$css: true
	}
};
var sizeStyles = {
	sm: {
		kAzted: "x1gdqauq",
		$$css: true
	},
	md: {
		kAzted: "x1uogy3y",
		$$css: true
	},
	lg: {
		kAzted: "x1xf4nls",
		$$css: true
	}
};
var endSectionSizeStyles = {
	sm: {
		k87sOh: "x1968x6z",
		k3aq6I: "x1cb1t30",
		$$css: true
	},
	md: {
		k87sOh: "xkmsrut",
		k3aq6I: "x1cb1t30",
		$$css: true
	},
	lg: {
		k87sOh: "x1oysc8t",
		k3aq6I: "x1cb1t30",
		$$css: true
	}
};
var truncatedSizeStyles = {
	sm: {
		kZKoxP: "x6k0iem",
		$$css: true
	},
	md: {
		kZKoxP: "x1ueg155",
		$$css: true
	},
	lg: {
		kZKoxP: "xssyfek",
		$$css: true
	}
};
var layerPlaceholderSizeStyles = {
	sm: {
		kZKoxP: "x6k0iem",
		$$css: true
	},
	md: {
		kZKoxP: "x1ueg155",
		$$css: true
	},
	lg: {
		kZKoxP: "xssyfek",
		$$css: true
	}
};
var CREATABLE_ID_PREFIX = "__xds_create__";
/**
* Multi-select input with token chips and typeahead search.
*
* Composes BaseTypeahead for search and Token for selected items.
* Tokens render inline before the text input. Selecting an item adds a token
* and clears the query. Backspace on empty input removes the last token.
*
* @example
* ```
* const [members, setMembers] = useState<UserItem[]>([]);
* <Tokenizer
*   label="Team members"
*   searchSource={userSource}
*   value={members}
*   onChange={(items, change) => {
*     setMembers(items);
*     if (change.type === 'add') {
*       console.log('Added:', change.item.label);
*     }
*   }}
*   placeholder="Search people..."
* />
* <Tokenizer
*   label="Tags"
*   searchSource={tagSource}
*   value={tags}
*   onChange={(items) => setTags(items)}
*   renderToken={(item, onRemove) => (
*     <Token
*       label={item.label}
*       color={item.auxiliaryData.color}
*       onRemove={onRemove}
*     />
*   )}
*   maxEntries={5}
* />
* ```
*/
function Tokenizer({ label, isLabelHidden = false, description, isRequired = false, isOptional = false, status, startIcon, labelTooltip, searchSource, value, onChange, renderItem, renderToken, maxEntries, placeholder, hasEntriesOnFocus, maxMenuItems, emptySearchResultsText, isDisabled = false, htmlName, disabledMessage, hasClear = false, endContent, hasAutoFocus, size: sizeProp, tokenOverflowBehavior = "none", debounceMs, hasCreate = false, onChangeQuery, onFocus, onBlur, width, xstyle, className, style, "data-testid": testId, ref, handleRef }) {
	const t = useTranslator();
	const size = useSize(sizeProp, "md");
	const inputId = (0, import_react.useId)();
	const descriptionId = (0, import_react.useId)();
	const statusMessageId = (0, import_react.useId)();
	const inputRef = (0, import_react.useRef)(null);
	const wrapperRef = (0, import_react.useRef)(null);
	const showsDisabledMessage = isDisabled && !!disabledMessage;
	const disabledMessageTooltip = useTooltip({
		placement: "above",
		focusTrigger: "always",
		isEnabled: showsDisabledMessage
	});
	(0, import_react.useImperativeHandle)(handleRef, () => ({
		focus() {
			inputRef.current?.focus();
		},
		blur() {
			inputRef.current?.blur();
		}
	}));
	const [isFocusedWithin, setIsFocusedWithin] = (0, import_react.useState)(false);
	const isTruncated = !isFocusedWithin && tokenOverflowBehavior !== "none" && value.length > 0;
	const isLayerMode = tokenOverflowBehavior === "unfocusedLayer";
	const layer = useLayer({ mode: "context" });
	const layerContentRef = (0, import_react.useRef)(null);
	const placeholderRef = (0, import_react.useCallback)((el) => {
		if (isLayerMode) layer.ref(el);
	}, [isLayerMode, layer]);
	const isFocusInTokenizer = (0, import_react.useCallback)((target) => {
		if (!target) return false;
		if (wrapperRef.current?.contains(target)) return true;
		if (layerContentRef.current?.contains(target)) return true;
		if (document.getElementById(layer.id)?.contains(target)) return true;
		return false;
	}, [layer.id]);
	const handleFocusCapture = (0, import_react.useCallback)((e) => {
		const comingFromOutside = !isFocusInTokenizer(e.relatedTarget);
		setIsFocusedWithin(true);
		if (isLayerMode) layer.show();
		if (comingFromOutside) {
			onFocus?.(e);
			if (e.target !== inputRef.current) inputRef.current?.focus();
		}
	}, [
		isLayerMode,
		layer,
		isFocusInTokenizer,
		onFocus
	]);
	const handleBlurCapture = (0, import_react.useCallback)((e) => {
		if (!isFocusInTokenizer(e.relatedTarget)) {
			setIsFocusedWithin(false);
			onBlur?.(e);
			if (isLayerMode) layer.hide();
		}
	}, [
		isLayerMode,
		layer,
		isFocusInTokenizer,
		onBlur
	]);
	const isAtMax = maxEntries != null && value.length >= maxEntries;
	const selectedIds = (0, import_react.useMemo)(() => new Set(value.map((item) => item.id)), [value]);
	const filteredSource = (0, import_react.useMemo)(() => ({
		search: async (query) => {
			const filtered = (await searchSource.search(query)).filter((item) => !selectedIds.has(item.id));
			if (hasCreate && query.trim()) {
				const trimmed = query.trim();
				if (!(selectedIds.has(trimmed) || filtered.some((item) => item.label.toLowerCase() === trimmed.toLowerCase()))) {
					const creatableItem = {
						id: `${CREATABLE_ID_PREFIX}${trimmed}`,
						label: `Create "${trimmed}"`,
						auxiliaryData: { __createdValue: trimmed }
					};
					filtered.push(creatableItem);
				}
			}
			return filtered;
		},
		bootstrap: async () => {
			return (await searchSource.bootstrap()).filter((item) => !selectedIds.has(item.id));
		}
	}), [
		searchSource,
		selectedIds,
		hasCreate
	]);
	const emptySource = (0, import_react.useMemo)(() => ({
		search: async () => [],
		bootstrap: async () => []
	}), []);
	const announce = useAnnounce();
	const handleAdd = (0, import_react.useCallback)((item) => {
		if (!item) return;
		if (isAtMax) return;
		if (hasCreate && typeof item.id === "string" && item.id.startsWith(CREATABLE_ID_PREFIX)) {
			const createdValue = item.id.slice(14);
			if (selectedIds.has(createdValue)) return;
			const realItem = {
				id: createdValue,
				label: createdValue
			};
			onChange([...value, realItem], {
				item: realItem,
				type: "create"
			});
			announce(`Added ${createdValue}`);
			return;
		}
		if (selectedIds.has(item.id)) return;
		onChange([...value, item], {
			item,
			type: "add"
		});
		announce(`Added ${item.label}`);
	}, [
		value,
		onChange,
		isAtMax,
		selectedIds,
		hasCreate,
		announce
	]);
	const handleRemove = (0, import_react.useCallback)((item) => {
		onChange(value.filter((v) => v.id !== item.id), {
			item,
			type: "remove"
		});
		announce(`Removed ${item.label}`);
		inputRef.current?.focus();
	}, [
		value,
		onChange,
		announce
	]);
	const handleClearAll = (0, import_react.useCallback)(() => {
		if (value.length === 0) return;
		const lastItem = value[value.length - 1];
		onChange([], {
			item: lastItem,
			type: "remove"
		});
		inputRef.current?.focus();
	}, [value, onChange]);
	const handleKeyDown = (0, import_react.useCallback)((e) => {
		if (e.key === "Backspace" && e.currentTarget.value === "" && value.length > 0) {
			e.preventDefault();
			const lastItem = value[value.length - 1];
			handleRemove(lastItem);
		}
	}, [value, handleRemove]);
	const handleWrapperClick = (0, import_react.useCallback)(() => {
		if (!isDisabled) if (isLayerMode) {
			layer.show();
			setIsFocusedWithin(true);
			inputRef.current?.focus();
		} else inputRef.current?.focus();
	}, [
		isDisabled,
		isLayerMode,
		layer
	]);
	const ariaDescribedBy = [
		description ? descriptionId : null,
		status?.message ? statusMessageId : null,
		showsDisabledMessage ? disabledMessageTooltip.describedBy : null
	].filter(Boolean).join(" ") || void 0;
	const sizeStyle = sizeStyles[size];
	const tokens = value.map((item) => {
		const onRemoveItem = () => handleRemove(item);
		if (renderToken) return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("span", {
			className: "x78zum5 x2lah0s",
			children: renderToken(item, onRemoveItem)
		}, item.id);
		return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Token, {
			label: item.label,
			size,
			onRemove: isDisabled ? void 0 : onRemoveItem,
			isDisabled,
			xstyle: styles.token
		}, item.id);
	});
	const popoverOverrideStyle = {
		top: "anchor(top)",
		left: "anchor(start)"
	};
	const wrapperContent = /*#__PURE__*/ (0, import_jsx_runtime.jsxs)("div", {
		ref: (el) => {
			wrapperRef.current = el;
			disabledMessageTooltip.ref(el);
		},
		role: "group",
		"aria-label": label,
		onClick: handleWrapperClick,
		onFocusCapture: handleFocusCapture,
		onBlurCapture: handleBlurCapture,
		"data-testid": testId,
		...mergeProps(themeProps("tokenizer", {
			size,
			status: status?.type
		}), props(inputWrapperStyles.base, styles.wrapper, value.length > 0 && styles.wrapperWithTokens, isTruncated ? truncatedSizeStyles[size] : sizeStyle, isTruncated && styles.truncatedWrapper, isDisabled && inputWrapperStyles.disabled, status && inputStatusBorderStyles[status.type], status && inputStatusHoverShadowStyles[status.type], status && inputStatusFocusWithinStyles[status.type])),
		children: [
			startIcon && /*#__PURE__*/ (0, import_jsx_runtime.jsx)("span", {
				...{
					0: {},
					1: { className: "x1xrm450" }
				}[!!(value.length > 0) << 0],
				children: renderIconSlot(startIcon, {
					size: "sm",
					color: "secondary"
				})
			}),
			isTruncated ? /*#__PURE__*/ (0, import_jsx_runtime.jsx)(OverflowList, {
				gap: 1,
				behavior: "observeParent",
				overflowRenderer: (items) => /*#__PURE__*/ (0, import_jsx_runtime.jsxs)("span", {
					className: "x2lah0s xuxw1ft x141an7d xv1l7n4 x7a5moj",
					children: [
						"+",
						items.length,
						" more"
					]
				}),
				children: tokens
			}) : tokens,
			/*#__PURE__*/ (0, import_jsx_runtime.jsx)(BaseTypeahead, {
				ref: inputRef,
				searchSource: isAtMax ? emptySource : filteredSource,
				value: null,
				onChange: handleAdd,
				renderItem,
				placeholder: value.length === 0 ? placeholder : "",
				hasEntriesOnFocus: isAtMax ? false : hasEntriesOnFocus,
				maxMenuItems,
				emptySearchResultsText,
				isDisabled,
				isFocusableDisabled: showsDisabledMessage,
				hasAutoFocus,
				inputId,
				ariaDescribedBy,
				onChangeQuery,
				debounceMs,
				onKeyDown: handleKeyDown,
				anchorRef: wrapperRef,
				size,
				inputXStyle: isAtMax || isTruncated ? styles.inputAtMax : value.length > 0 ? styles.inputCompact : void 0
			}),
			htmlName != null && value.map((item) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)("input", {
				type: "hidden",
				name: htmlName,
				value: item.id,
				disabled: isDisabled
			}, item.id)),
			(endContent || hasClear && value.length > 0 && !isDisabled) && /*#__PURE__*/ (0, import_jsx_runtime.jsxs)("div", {
				...props(styles.endSection, endSectionSizeStyles[size]),
				children: [endContent, hasClear && value.length > 0 && !isDisabled && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(InputClearButton, {
					label: t("@astryx.tokenizer.clearAll"),
					onClick: (e) => {
						e.stopPropagation();
						handleClearAll();
					}
				})]
			})
		]
	});
	let tokenizerContent;
	if (isLayerMode) {
		const placeholderSizeStyle = layerPlaceholderSizeStyles[size];
		tokenizerContent = /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)("div", {
			ref: placeholderRef,
			onClick: handleWrapperClick,
			...mergeProps(themeProps("tokenizer", {
				size,
				status: status?.type
			}), props(inputWrapperStyles.base, styles.wrapper, value.length > 0 && styles.wrapperWithTokens, placeholderSizeStyle, isTruncated && styles.truncatedWrapper, isDisabled && inputWrapperStyles.disabled, status && inputStatusBorderStyles[status.type], status && inputStatusHoverShadowStyles[status.type], status && inputStatusFocusWithinStyles[status.type])),
			children: isTruncated && /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [startIcon && renderIconSlot(startIcon, {
				size: "sm",
				color: "secondary"
			}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(OverflowList, {
				gap: 1,
				behavior: "observeParent",
				overflowRenderer: (items) => /*#__PURE__*/ (0, import_jsx_runtime.jsxs)("span", {
					className: "x2lah0s xuxw1ft x141an7d xv1l7n4 x7a5moj",
					children: [
						"+",
						items.length,
						" more"
					]
				}),
				children: tokens
			})] })
		}), layer.render(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("div", {
			ref: layerContentRef,
			onFocusCapture: handleFocusCapture,
			onBlurCapture: handleBlurCapture,
			children: wrapperContent
		}), {
			positioning: "custom",
			xstyle: styles.layerPopover,
			style: popoverOverrideStyle
		})] });
	} else tokenizerContent = wrapperContent;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(Field, {
		ref,
		label,
		isLabelHidden,
		description,
		inputID: inputId,
		descriptionID: description ? descriptionId : void 0,
		isOptional,
		isRequired,
		isDisabled,
		status: status ? {
			type: status.type,
			message: status.message,
			messageID: status.message ? statusMessageId : void 0
		} : void 0,
		labelTooltip,
		width,
		xstyle,
		className,
		style,
		children: [tokenizerContent, showsDisabledMessage && disabledMessageTooltip.renderTooltip(disabledMessage)]
	});
}
Tokenizer.displayName = "Tokenizer";
//#endregion
export { InputClearButton as i, BaseTypeahead as n, TypeaheadItem as r, Tokenizer as t };
